#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "Item.h"
#include "set.h"

int main()
{
    /*Item prova;
    prova=ITEMscan("Primo Nodo",6);
    ITEMshow(prova);

    Item prova2;
    prova2=ITEMscan("Secondo Nodo",14);
    ITEMshow(prova2);

    Item prova3;
    prova3=ITEMscan("Terzo Nodo",16);
    ITEMshow(prova3);

    link head=NULL;

    head=list_insert_head(head,prova2);
    list_show(head);
    list_insert_head_P(&head,prova);
    list_show(head);

    head=list_insert_tail(head, prova3);
    list_show(head);

    Item trovato=list_search(head, 16);
    ITEMshow(trovato);

    head=list_delete_head(head);
    list_show(head);
*/
    return 0;
}

